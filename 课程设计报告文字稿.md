# 在线匿名聊天室课程设计报告（文字稿）

> 使用说明（不属于报告正文，请勿复制进 Word）：
> 1. 按模板排版：每章独立分页；章标题小二号黑体居中，节标题小三号黑体顶头，小节标题小四号黑体顶头；正文小四号宋体、1.5 倍行距；页眉「数据库课程设计」。
> 2. 正文从第一章开始编阿拉伯数字页码，目录用罗马数字。
> 3. 标注【截图：xxx】【图x-x】【表x-x】处请自行插入截图/绘制图表；图题在图下方居中，表题在表上方居中。
> 4. 目录请在 Word 中根据最终标题自动生成，与正文标题保持一致（三级标题）。
> 5. 封面「选题：在线匿名聊天室系统」，学生/学号/专业/班级/指导教师自行填写，日期 2026 年 11 月。

---

（以下为报告正文文字稿）

---

# 封面

**数据库原理与应用课程设计**

| 项目 | 内容 |
| --- | --- |
| 选题 | 在线匿名聊天室系统 |
| 学生 | （填写） |
| 学号 | （填写） |
| 专业 | 人工智能 |
| 班级 | （填写） |
| 指导教师 | （填写） |

2026 年 11 月

---

# 目录

（在 Word 中按三级标题自动生成，示例结构如下）

```
目录

第一章 系统概述
    1.1 系统开发背景
    1.2 系统功能需求分析
    1.3 系统性能需求分析
    1.4 系统数据流程分析
第二章 数据库概念结构设计
    2.1 实体分析
    2.2 局部 E-R 图
    2.3 全局 E-R 图
第三章 数据库逻辑结构设计
    3.1 实体向关系模式的转换
    3.2 联系向关系模式的转换
    3.3 规范化说明（3NF）
第四章 数据库物理结构设计
    4.1 数据库及数据表的创建
    4.2 数据完整性设计
    4.3 索引的创建
    4.4 视图的创建
    4.5 数据库的操作
第五章 应用程序设计
    5.1 开发平台与总体架构
    5.2 数据库连接模块
    5.3 用户登录模块
    5.4 数据插入：发送聊天消息
    5.5 数据浏览与修改：房间管理
    5.6 表格浏览、修改与删除：消息管理
    5.7 综合查询：消息统计报表
    5.8 系统运行截图
    5.9 小组分工
结束语
参考文献
```

---

# 第一章 系统概述

## 1.1 系统开发背景

随着局域网办公与校园网络的普及，团队内部即时沟通的需求日益增长。商用即时通讯工具往往依赖互联网与中心化账号体系，存在隐私暴露、需实名注册、局域网断外网即不可用等问题。在线匿名聊天室系统面向宿舍、实验室、课堂等局域网场景，使用者无需注册真实身份，以匿名昵称加入房间进行文字、图片与文件交流，既满足即时沟通需求，又保护使用者隐私。

本系统是一个典型的"应用程序 + 数据库"综合实践项目：前台采用 Python Flask 框架实现单端口多房间路由与 Server-Sent Events（SSE）实时推送，后台采用 MySQL 8.0 关系数据库存储房间注册信息、用户档案、聊天消息、收藏、黑名单与审计日志。通过本课题，将数据库需求分析、概念结构设计、逻辑结构设计、物理结构设计与 SQL 编程（约束、索引、视图、存储过程、触发器）完整落地到一个可运行的应用系统中，体现了数据库原理与应用课程的核心要求。

系统需要解决的主要问题如下：

1. 多房间隔离：多个聊天房间并行存在，消息、档案、文件互不串扰，同时又要便于统一管理与统计。
2. 匿名会话：用户不注册账号，凭设备标识复用头像昵称档案，每次进入生成随机会话身份。
3. 数据完整性与安全性：房间开关、密码、禁言、黑名单、撤回时限等规则必须由数据库约束与业务逻辑共同保证。
4. 管理可追溯：管理员删除消息、用户撤回消息等破坏性操作需要留下审计痕迹。
5. 统计与报表：管理端需要对消息总量、按用户、按类型进行综合查询统计。

论文内容安排：第一章概述系统与需求；第二、三、四章分别完成数据库概念、逻辑、物理结构设计；第五章给出应用程序各模块设计、关键代码与运行截图；最后为结束语与参考文献。

## 1.2 系统功能需求分析

系统用户分为两类角色：普通用户与管理员。管理员凭密码登录后拥有全部权限；普通用户无需密码直接进入。不同角色登录后看到的功能界面不同。

系统业务流程为：用户打开入口页面 → 选择身份登录 → 浏览房间列表 → 输入昵称加入房间（有密码房间需验证密码）→ 匿名聊天（文字/图片/文件）、收藏消息、限时撤回自己的消息 → 离开房间。管理员在此基础上还可创建、开关、删除房间，修改房间名称/密码/文件限额/保留条数/撤回时限，强制删除任意消息，禁言、踢出在线用户，查看消息统计报表与删除审计日志，以及全局恢复出厂。

按功能模块划分如表 1-1 所示。

**表 1-1 系统功能模块一览**

| 模块 | 子功能 | 使用角色 |
| --- | --- | --- |
| 登录模块 | 用户免密登录、管理员密码登录、退出登录 | 所有访问者 |
| 房间列表模块 | 浏览房间、查看开放状态、扫码进入 | 用户、管理员 |
| 房间管理模块 | 创建房间、开关房间、删除房间、房间二维码 | 管理员 |
| 聊天核心模块 | 加入/离开房间、发送文字图片文件、SSE 实时推送、历史消息 | 用户、管理员 |
| 互动模块 | 收藏/取消收藏消息、限时撤回自己的消息 | 用户 |
| 房间配置模块 | 改名、设置密码、文件限额、保留条数、撤回时限、清空记录、房间恢复出厂 | 房间内管理员 |
| 用户管理模块 | 在线用户列表、禁言/解除禁言、踢出并拉黑、黑名单管理 | 房间内管理员 |
| 统计审计模块 | 消息综合统计报表、消息删除审计日志 | 房间内管理员 |
| 系统管理模块 | 修改管理员密码、全局恢复出厂 | 管理员 |

【图1-1 系统功能模块图】（请按表 1-1 绘制树状功能模块图）

系统用例可概括为：普通用户对应"登录、浏览房间、加入房间、收发消息、上传下载文件、收藏、撤回、离开"等用例；管理员额外对应"创建/删除/开关房间、修改房间配置、强制删消息、禁言踢出、查看统计与审计、全局复位"等用例；系统本身对应"自动裁剪旧消息、删除消息写审计、房间删光自动重建默认房间"等用例。

【图1-2 系统用例图】

## 1.3 系统性能需求分析

1. 安全性：管理员接口与房间管理接口必须经过会话鉴权，未登录或角色不符一律返回 403；房间内管理操作还需校验"当前在线管理员身份"，防止伪造 user_id 越权；数据库账号与密码通过常量/环境变量集中配置，不硬编码在业务代码中；上传文件执行扩展名白名单清洗与大小限制，防止恶意文件落地。
2. 完整性：数据库层通过主键、外键级联、UNIQUE、CHECK、DEFAULT 约束保证实体完整性、参照完整性与用户定义完整性；业务层通过事务性单连接写入与锁机制保证并发下的一致性。
3. 可操作性：界面全部为浏览器访问，无需安装客户端；入口页与每个房间提供二维码，手机扫码即可进入；匿名加入只需输入昵称，操作成本低。
4. 实时性：采用 SSE 长连接推送消息与在线列表，30 秒心跳保活，消息发送后所有在线端毫秒级同步。
5. 可维护性：全部房间共用一个 MySQL 库，以 room_id 区分作用域，备份与统计只需针对单一数据库；完整 DDL 沉淀为 sql/schema.sql 脚本，可一键重建。
6. 性能：消息表按 (room_id, timestamp) 建立复合索引，取"最近 N 条消息"与按房间统计均为索引覆盖范围内的查询；单房间千条量级下响应在毫秒级。

## 1.4 系统数据流程分析

系统的数据流可以描述为：输入 → 处理 → 存储 → 输出。

1. 输入数据：登录请求（角色、管理员密码）、房间管理命令（房间名、开关状态、各类限额）、加入请求（昵称、设备 ID、房间密码）、消息内容（文本/文件 URL）、收藏与撤回指令、文件二进制流。
2. 处理过程：Flask 路由层解析请求并完成会话鉴权；ChatRoom 业务层执行开放状态、密码、黑名单、禁言、昵称唯一等校验；Database/DatabaseManager 数据访问层将业务对象映射为 SQL 语句写入或读出 MySQL；广播层将事件序列化后经 SSE 队列分发给房间内所有在线客户端。
3. 存储数据：rooms 房间注册表、config 全局配置表、profiles 用户档案表、messages 聊天消息表、favorites 收藏表、blacklist 黑名单表、message_audit 删除审计表，共 7 张基本表；另有 2 个统计视图、1 个审计触发器、3 个存储过程。
4. 输出数据：聊天页历史消息与实时消息流、在线用户列表、房间列表页、消息统计报表（总数/按用户/按类型）、审计日志列表、房间二维码图片。

【图1-3 系统顶层数据流图（第0层）】（建议绘制：外部实体"用户/管理员"与"系统"之间的四条主数据流：登录信息、房间与消息数据、查询统计请求、消息与报表输出）

数据字典（主要数据项）如表 1-2 所示。

**表 1-2 数据字典（主要数据项）**

| 数据项 | 含义 | 类型 | 来源/去向 |
| --- | --- | --- | --- |
| room_id | 房间唯一标识，形如 room_时间戳_随机串 | 字符串(64) | 系统生成 → rooms 表 |
| room_name | 房间显示名称 | 字符串(100) | 管理员输入 → rooms 表 |
| device_id | 浏览器设备标识，跨会话稳定 | 字符串(64) | 前端生成 → profiles/blacklist 表 |
| user_id | 单次进入房间的匿名会话标识 | 字符串(64) | 加入时随机生成 → 内存在线表、messages.user_id |
| nickname | 房间内匿名昵称 | 字符串(50) | 用户输入 → profiles 表 |
| message content | 消息正文或上传文件 URL | 文本 | 用户输入 → messages 表 |
| msg type | 消息类型 text/image/file/system | 枚举字符串 | 系统判定 → messages.type |
| recall_time_limit | 撤回时限（秒，0 表示禁止） | 整数 | 管理员设置 → rooms 表 |
| admin_password | 管理员登录密码 | 文本 | 管理员设置 → config 表 |

---

# 第二章 数据库概念结构设计

## 2.1 实体分析

通过对系统业务的分析，识别出以下实体及其主要属性：

1. 房间（Room）：房间号、房间名、开放状态、密码、单文件限额、消息保留条数、撤回时限、创建时间。
2. 用户档案（Profile）：设备号、昵称、头像、颜色、禁言截止时间（同一房间内一台设备对应一条档案）。
3. 消息（Message）：消息序号、消息 ID、类型、内容、发送者昵称、会话标识、是否管理员、时间戳、文件名、文件大小。
4. 收藏（Favorite）：收藏序号、用户标识、被收藏消息的快照信息、收藏时间。
5. 黑名单（Blacklist）：设备号、加入黑名单时的昵称、IP、加入时间。
6. 删除审计（Audit）：审计序号、被删消息的快照、删除时间、操作类型。
7. 全局配置（Config）：配置键、配置值（存放管理员密码等）。

实体间的主要联系：

1. 房间与消息：1:n（一个房间包含多条消息）。
2. 房间与用户档案：1:n（一个房间拥有多个用户的档案；档案以房间+设备为粒度，跨房间不共享）。
3. 房间与黑名单、房间与收藏：1:n。
4. 用户（会话）与消息：1:n（一个会话可发送多条消息）。
5. 用户（会话）与收藏：1:n；收藏与消息：n:1（收藏时对消息内容做快照冗余，逻辑上仍指向原消息）。
6. 消息与删除审计：1:1（消息每被删除一次产生一条审计记录，审计为弱实体性质的流水记录）。
7. 全局配置与其余实体无联系（独立的键值实体）。

## 2.2 局部 E-R 图

【图2-1 房间—消息—档案 局部 E-R 图】（矩形实体、椭圆属性、菱形联系，联系上标注 1:n）

【图2-2 用户—收藏—消息 局部 E-R 图】（标注 n:1 与 1:n）

【图2-3 消息—删除审计 局部 E-R 图】（标注 1:1）

各局部图的绘制要点：房间与消息之间菱形"发布"为 1:n，n 端消息继承主键 room_id；用户档案与房间之间"拥有"为 1:n，联系属性含昵称、头像等（也可作为档案实体属性直接画在实体上）；收藏联系将用户与消息连接，m:n 语义上因做快照而退化为以 (room_id, user_id, msg_id) 唯一的 1:1 关系记录。

## 2.3 全局 E-R 图

将各局部 E-R 图合并，消除冗余属性与冗余联系，得到全局 E-R 图如图 2-4 所示。

【图2-4 系统全局 E-R 图】

合并说明：三个 1:n 联系的"n"端实体（消息、档案、黑名单、收藏）均以 room_id 作为外键归属房间，避免引入多余关系；配置实体独立放置；审计实体只与消息相连，不与房间直接相连外键（设计上故意不设外键，使房间删除后审计仍可事后清查，详见 4.2.3）。

---

# 第三章 数据库逻辑结构设计

## 3.1 实体向关系模式的转换

将全局 E-R 图中的每个实体转换为一个关系模式，实体名作为关系名，属性作为列，实体的键即关系的主键（下划线标出关键字）。

1. 房间（直接转换，1 个实体 → 1 个关系）：

   房间（<u>room_id</u>，room_name，is_open，password，file_limit_mb，max_history，recall_time_limit，created_at）

2. 用户档案（直接转换，复合主键来自"房间 1:n 档案"联系的实现需要）：

   用户档案（<u>room_id，device_id</u>，nickname，avatar，color，muted_until）

3. 全局配置（直接转换）：

   配置（<u>key</u>，value）

## 3.2 联系向关系模式的转换

1. 1:n 联系的转换（房间—消息、房间—黑名单、房间—收藏、会话—消息、会话—收藏）：
   1:n 联系的常规做法是"在 n 端实体的关系中加入 1 端实体的主键作为外键"。消息、黑名单、收藏三个 n 端均增加 room_id 外键引用房间；消息 additionally 增加 user_id 记录发送会话，收藏增加 user_id 记录收藏者。转换结果：

   消息（<u>seq</u>，<u>room_id，id</u>，type，content，sender，user_id，is_admin，avatar，color，timestamp，file_name，file_size）
   其中 room_id 为外键，(room_id, id) 设唯一键防止消息 ID 重复；seq 为代理主键，保证"按插入顺序裁剪旧消息"的需要。

   黑名单（<u>room_id，device_id</u>，nickname，ip，created_at）

   收藏（<u>id</u>，room_id，user_id，msg_id，msg_type，content，sender，file_name，file_size，created_at）
   其中 (room_id, user_id, msg_id) 设唯一键实现"同一用户对同一消息只收藏一次"；content 等字段为消息快照冗余，属于受控反规范化，便于原消息被撤回后收藏仍可展示。

2. 1:1 联系的转换（消息—删除审计）：
   审计为流水型弱实体，采用独立关系模式并以自动增长 audit_id 为主键，冗余保存被删消息的 room_id、message_id 及内容快照，不设指向 messages 的外键（消息删除后主键行已不存在，外键必然失败；且希望房间删除后审计仍可查）。转换结果：

   删除审计（<u>audit_id</u>，room_id，message_id，type，content，sender，user_id，deleted_at，action）

3. m:n 联系的说明：
   本系统严格意义上的 m:n 联系（用户与消息的"发送"关系已由消息表的 user_id 属性表达为 1:n）在建模时均通过引入中间属性或快照表化解，未额外产生纯关联表，减少了查询时的连接开销。

## 3.3 规范化说明（3NF）

各关系模式均满足第三范式要求，分析如下：

1. 满足 1NF：所有字段均为不可再分的原子值；消息正文、配置值使用 TEXT，仍视为单一属性值。
2. 满足 2NF：各模式均为单属性或复合主键，不存在非主属性对码的部分函数依赖。例如消息表以 seq 为码时主键单属性；档案表以 (room_id, device_id) 为码，nickname、avatar 等完全函数依赖于整个码（头像颜色按"房间+设备"维度分配，跨房间可不同）。
3. 满足 3NF：不存在非主属性对码的传递函数依赖。房间表中 password、各类限额均直接依赖 room_id；档案表 muted_until 直接依赖 (room_id, device_id)；收藏快照字段依赖于收藏主键 id（msg_id 不再决定 content 的独立传递链以外的内容）。
4. 受控冗余说明：收藏表保存消息快照、审计表保存被删消息快照，属于为业务连续性（原消息已删仍可查看收藏/审计）而有意引入的冗余，通过触发器/应用层单点写入保证一致性，不破坏整体设计的规范性。

最终关系模式汇总（关键字下划线）：

- 房间（<u>room_id</u>，room_name，is_open，password，file_limit_mb，max_history，recall_time_limit，created_at）
- 配置（<u>key</u>，value）
- 用户档案（<u>room_id，device_id</u>，nickname，avatar，color，muted_until）—— 外键 room_id → 房间
- 消息（<u>seq</u>，room_id，<u>id</u>，type，content，sender，user_id，is_admin，avatar，color，timestamp，file_name，file_size）—— 外键 room_id → 房间
- 黑名单（<u>room_id，device_id</u>，nickname，ip，created_at）—— 外键 room_id → 房间
- 收藏（<u>id</u>，room_id，user_id，msg_id，msg_type，content，sender，file_name，file_size，created_at）—— 外键 room_id → 房间
- 删除审计（<u>audit_id</u>，room_id，message_id，type，content，sender，user_id，deleted_at，action）—— 无外键（故意）

---

# 第四章 数据库物理结构设计

本章全部 SQL 语句与项目中 sql/schema.sql 脚本一致，运行环境为 MySQL 8.0，字符集统一使用 utf8mb4，排序规则统一使用 utf8mb4_unicode_ci（全库、全表、存储过程参数保持同一 collation，避免"Illegal mix of collations"错误）。

## 4.1 数据库及数据表的创建

### 4.1.1 数据库创建

```sql
-- 创建数据库 lan_chat，字符集 utf8mb4，排序规则 utf8mb4_unicode_ci
CREATE DATABASE IF NOT EXISTS lan_chat
    DEFAULT CHARACTER SET utf8mb4
    DEFAULT COLLATE utf8mb4_unicode_ci;

USE lan_chat;
```

【截图：4-1 数据库创建成功】（在 MySQL 客户端执行后 `SHOW CREATE DATABASE lan_chat;` 或 Navicat 数据库列表截图）

### 4.1.2 数据表的创建

本系统共 7 张基本表。其中 rooms 表、messages 表建议在图形化工具（Navicat/MySQL Workbench）中设计表结构，其余表用 SQL 语句创建。

rooms 房间注册表（图形工具设计）结构如表 4-1 所示。

**表 4-1 rooms 表结构**

| 字段名 | 数据类型 | 约束 | 说明 |
| --- | --- | --- | --- |
| room_id | VARCHAR(64) | PRIMARY KEY | 房间号 |
| room_name | VARCHAR(100) | NOT NULL, DEFAULT '新房间' | 房间名 |
| is_open | TINYINT | NOT NULL, DEFAULT 1, CHECK (0,1) | 是否开放 |
| password | VARCHAR(255) | DEFAULT NULL | 房间密码，空为无密码 |
| file_limit_mb | INT | NOT NULL, DEFAULT 0, CHECK >=0 | 单文件 MB 上限，0 不限 |
| max_history | INT | NOT NULL, DEFAULT 200, CHECK >=10 | 消息保留条数 |
| recall_time_limit | INT | NOT NULL, DEFAULT 300, CHECK >=0 | 撤回时限秒，0 禁止 |
| created_at | DOUBLE | DEFAULT NULL | 创建时间（Unix 时间戳） |

【截图：4-2 图形工具创建 rooms 表】

以下为 SQL 方式创建的其余表（含注释）：

```sql
-- 全局键值配置表：存放 admin_password 等系统配置
CREATE TABLE IF NOT EXISTS config (
    `key`   VARCHAR(64) NOT NULL,
    `value` TEXT,
    PRIMARY KEY (`key`)
) ENGINE = InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

-- 用户档案表：同一房间内一台设备一条档案（房间+设备 联合主键）
CREATE TABLE IF NOT EXISTS profiles (
    room_id      VARCHAR(64) NOT NULL,
    device_id    VARCHAR(64) NOT NULL,
    nickname     VARCHAR(50) NOT NULL,
    avatar       VARCHAR(16),
    color        VARCHAR(16),
    muted_until  DOUBLE      NOT NULL DEFAULT 0,
    PRIMARY KEY (room_id, device_id),
    CONSTRAINT fk_profiles_room FOREIGN KEY (room_id)
        REFERENCES rooms (room_id) ON DELETE CASCADE,
    CONSTRAINT chk_profiles_mute CHECK (muted_until >= 0),
    INDEX idx_profiles_nickname (room_id, nickname)
) ENGINE = InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

-- 聊天消息表：seq 代理主键保证插入序，供"最近N条/裁剪"使用
CREATE TABLE IF NOT EXISTS messages (
    seq         BIGINT       NOT NULL AUTO_INCREMENT,
    room_id     VARCHAR(64)  NOT NULL,
    id          VARCHAR(64)  NOT NULL,
    type        VARCHAR(16),
    content     TEXT,
    sender      VARCHAR(50),
    user_id     VARCHAR(64),
    is_admin    TINYINT      NOT NULL DEFAULT 0,
    avatar      VARCHAR(16),
    color       VARCHAR(16),
    timestamp   DOUBLE,
    file_name   VARCHAR(100),
    file_size   BIGINT,
    PRIMARY KEY (seq),
    UNIQUE KEY uk_messages_room_id (room_id, id),
    CONSTRAINT fk_messages_room FOREIGN KEY (room_id)
        REFERENCES rooms (room_id) ON DELETE CASCADE,
    CONSTRAINT chk_messages_type CHECK (
        type IS NULL OR type IN ('text', 'image', 'file', 'system')
    ),
    CONSTRAINT chk_messages_admin CHECK (is_admin IN (0, 1)),
    INDEX idx_messages_room_time (room_id, timestamp),
    INDEX idx_messages_room_sender (room_id, sender)
) ENGINE = InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

-- 黑名单表：按设备拉黑，房间删除则级联清除
CREATE TABLE IF NOT EXISTS blacklist (
    room_id    VARCHAR(64) NOT NULL,
    device_id  VARCHAR(64) NOT NULL,
    nickname   VARCHAR(50),
    ip         VARCHAR(45),
    created_at DOUBLE,
    PRIMARY KEY (room_id, device_id),
    CONSTRAINT fk_blacklist_room FOREIGN KEY (room_id)
        REFERENCES rooms (room_id) ON DELETE CASCADE,
    INDEX idx_blacklist_created (created_at)
) ENGINE = InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

-- 收藏表：唯一键保证同一用户对同一消息只收藏一次
CREATE TABLE IF NOT EXISTS favorites (
    id         BIGINT       NOT NULL AUTO_INCREMENT,
    room_id    VARCHAR(64)  NOT NULL,
    user_id    VARCHAR(64)  NOT NULL,
    msg_id     VARCHAR(64),
    msg_type   VARCHAR(16),
    content    TEXT,
    sender     VARCHAR(50),
    file_name  VARCHAR(100),
    file_size  BIGINT,
    created_at DOUBLE       NOT NULL,
    PRIMARY KEY (id),
    UNIQUE KEY uk_favorites_user_msg (room_id, user_id, msg_id),
    CONSTRAINT fk_favorites_room FOREIGN KEY (room_id)
        REFERENCES rooms (room_id) ON DELETE CASCADE,
    INDEX idx_favorites_user (room_id, user_id, created_at)
) ENGINE = InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

-- 消息删除审计表：由触发器写入；故意不设外键，房间删除后仍可事后清查
CREATE TABLE IF NOT EXISTS message_audit (
    audit_id   BIGINT       NOT NULL AUTO_INCREMENT,
    room_id    VARCHAR(64)  NOT NULL,
    message_id VARCHAR(64),
    type       VARCHAR(16),
    content    TEXT,
    sender     VARCHAR(50),
    user_id    VARCHAR(64),
    deleted_at DOUBLE,
    action     VARCHAR(16),
    PRIMARY KEY (audit_id),
    INDEX idx_audit_room_time (room_id, deleted_at)
) ENGINE = InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

【截图：4-3 数据表创建成功】（`SHOW TABLES;` 与 `SHOW CREATE TABLE messages;` 结果截图）

## 4.2 数据完整性设计

### 4.2.1 主键约束的创建

每张表均声明主键保证实体完整性。单属性主键示例（rooms）：

```sql
ALTER TABLE rooms ADD PRIMARY KEY (room_id);
```

复合主键示例（profiles，房间+设备唯一确定一条档案）：

```sql
ALTER TABLE profiles ADD PRIMARY KEY (room_id, device_id);
```

图形工具方式：在 Navicat 设计表界面勾选"键"列的主键图标即可，等价于上述 SQL。

### 4.2.2 DEFAULT 约束的创建

```sql
-- 房间默认开放、默认保留 200 条、默认撤回时限 300 秒
ALTER TABLE rooms
    MODIFY is_open TINYINT NOT NULL DEFAULT 1,
    MODIFY max_history INT NOT NULL DEFAULT 200,
    MODIFY recall_time_limit INT NOT NULL DEFAULT 300;

-- 档案默认未禁言（muted_until=0）
ALTER TABLE profiles
    MODIFY muted_until DOUBLE NOT NULL DEFAULT 0;
```

说明：0 是"禁止撤回/未禁言"的合法业务值，DEFAULT 0 与应用层"读取后按空值兜底"配合，保证重启后配置不漂移。

### 4.2.3 外键约束的创建

```sql
-- 消息归属房间，房间删除时级联删除其消息
ALTER TABLE messages
    ADD CONSTRAINT fk_messages_room FOREIGN KEY (room_id)
        REFERENCES rooms (room_id) ON DELETE CASCADE;

-- 档案、黑名单、收藏同理级联
ALTER TABLE profiles
    ADD CONSTRAINT fk_profiles_room FOREIGN KEY (room_id)
        REFERENCES rooms (room_id) ON DELETE CASCADE;
ALTER TABLE blacklist
    ADD CONSTRAINT fk_blacklist_room FOREIGN KEY (room_id)
        REFERENCES rooms (room_id) ON DELETE CASCADE;
ALTER TABLE favorites
    ADD CONSTRAINT fk_favorites_room FOREIGN KEY (room_id)
        REFERENCES rooms (room_id) ON DELETE CASCADE;
```

设计权衡：message_audit（审计表）不建外键。原因有二：其一，审计记录引用的 message_id 所对应的消息行已被删除，指向 messages 的外键必然失败；其二，希望"删除房间后仍能事后清查该房间曾删除过哪些消息"。因此审计表的 room_id 仅作普通字段并在删除房间时由应用程序显式清理（全局恢复出厂）或保留（单房间删除场景下随业务决策处理），这是在完整性与可审计性之间作出的有意取舍，并在系统设计文档中明确记录。

### 4.2.4 UNIQUE 约束的创建

```sql
-- 同一房间内消息 ID 不重复（防重复插入/重复撤回误伤）
ALTER TABLE messages
    ADD CONSTRAINT uk_messages_room_id UNIQUE (room_id, id);

-- 同一用户在同一房间对同一消息只能收藏一次
ALTER TABLE favorites
    ADD CONSTRAINT uk_favorites_user_msg UNIQUE (room_id, user_id, msg_id);
```

应用层对应：收藏插入使用 `INSERT IGNORE`，重复收藏时 affected rows 为 0 而不报错。

### 4.2.5 CHECK 约束的创建

```sql
-- 开关字段只能是 0/1
ALTER TABLE rooms ADD CONSTRAINT chk_rooms_open CHECK (is_open IN (0, 1));
-- 文件限额非负
ALTER TABLE rooms ADD CONSTRAINT chk_rooms_file_limit CHECK (file_limit_mb >= 0);
-- 保留条数至少 10 条
ALTER TABLE rooms ADD CONSTRAINT chk_rooms_max_history CHECK (max_history >= 10);
-- 撤回时限非负（0 = 禁止撤回）
ALTER TABLE rooms ADD CONSTRAINT chk_rooms_recall CHECK (recall_time_limit >= 0);
-- 消息类型白名单
ALTER TABLE messages ADD CONSTRAINT chk_messages_type
    CHECK (type IS NULL OR type IN ('text', 'image', 'file', 'system'));
-- 禁言截止时间非负
ALTER TABLE profiles ADD CONSTRAINT chk_profiles_mute CHECK (muted_until >= 0);
```

【截图：4-4 违反 CHECK 约束报错示例】（向 is_open 插入 2 应返回错误）

## 4.3 索引的创建

遵循"每表至少一个合适索引"的原则，结合查询模式建索引如下：

1. messages 表：`INDEX idx_messages_room_time (room_id, timestamp)`——支撑"取房间最近 N 条消息"与按时间排序浏览；`INDEX idx_messages_room_sender (room_id, sender)`——支撑按昵称分组检索消息。

   ```sql
   ALTER TABLE messages
       ADD INDEX idx_messages_room_time (room_id, timestamp),
       ADD INDEX idx_messages_room_sender (room_id, sender);
   ```

2. profiles 表：`INDEX idx_profiles_nickname (room_id, nickname)`——支撑按昵称查找/校验档案。

   ```sql
   ALTER TABLE profiles ADD INDEX idx_profiles_nickname (room_id, nickname);
   ```

3. favorites 表：`INDEX idx_favorites_user (room_id, user_id, created_at)`——支撑"我的收藏列表"按时间倒序查询。

   ```sql
   ALTER TABLE favorites ADD INDEX idx_favorites_user (room_id, user_id, created_at);
   ```

4. blacklist 表：`INDEX idx_blacklist_created (created_at)`——支撑按拉黑时间审计清理。

   ```sql
   ALTER TABLE blacklist ADD INDEX idx_blacklist_created (created_at);
   ```

5. message_audit 表：`INDEX idx_audit_room_time (room_id, deleted_at)`——支撑审计日志按房间与时间查询。

   ```sql
   ALTER TABLE message_audit ADD INDEX idx_audit_room_time (room_id, deleted_at);
   ```

【截图：4-5 查看索引】（`SHOW INDEX FROM messages;` 结果截图）

## 4.4 视图的创建

视图 1：v_message_stats——按用户统计消息数与活跃时间。

```sql
CREATE OR REPLACE VIEW v_message_stats AS
SELECT room_id,
       user_id,
       sender,
       COUNT(*)           AS message_count,
       MAX(`timestamp`)   AS last_active,
       MIN(`timestamp`)   AS first_active
FROM messages
WHERE user_id IS NOT NULL
GROUP BY room_id, user_id, sender;
```

视图 2：v_message_type_stats——按消息类型统计数量。

```sql
CREATE OR REPLACE VIEW v_message_type_stats AS
SELECT room_id,
       type,
       COUNT(*) AS count
FROM messages
GROUP BY room_id, type;
```

两个视图被存储过程 sp_get_message_stats 复用，同时可直接在客户端查询，供管理报表使用。

【截图：4-6 视图创建成功与查询结果】（`SHOW CREATE VIEW v_message_stats;` 及 `SELECT * FROM v_message_stats;`）

## 4.5 数据库的操作

### 4.5.1 数据操纵

（1）插入数据——创建房间时插入 rooms 一行：

```sql
INSERT INTO rooms (room_id, room_name, is_open, password,
                   file_limit_mb, max_history, recall_time_limit, created_at)
VALUES ('room_default', '在线匿名聊天室', 1, NULL, 0, 200, 300, UNIX_TIMESTAMP());
```

（2）更新数据——管理员修改房间配置（如将撤回时限设为 0 表示禁止撤回）：

```sql
UPDATE rooms
SET recall_time_limit = 0
WHERE room_id = 'room_default';
```

（3）删除数据——删除某房间（其余业务表随外键级联删除）：

```sql
DELETE FROM rooms WHERE room_id = 'test_room';
```

（4）表结构修改——给消息表补充文件大小字段语义时可演示 ALTER：

```sql
ALTER TABLE messages MODIFY file_size BIGINT COMMENT '文件字节数，非文件消息为 NULL';
```

【截图：4-7 数据插入/更新/删除执行成功】（各语句 affected rows 截图）

### 4.5.2 单表/多表数据查询

单表查询示例——查询某房间最近 10 条文本消息：

```sql
SELECT id, sender, content, `timestamp`
FROM messages
WHERE room_id = 'room_default' AND type = 'text'
ORDER BY seq DESC
LIMIT 10;
```

单表查询示例——统计某房间各类型消息数：

```sql
SELECT type, COUNT(*) AS cnt
FROM messages
WHERE room_id = 'room_default'
GROUP BY type
ORDER BY cnt DESC;
```

多表查询示例——统计每个房间的消息总数与档案数（rooms LEFT JOIN 聚合子查询）：

```sql
SELECT r.room_id,
       r.room_name,
       r.is_open,
       COALESCE(m.cnt, 0) AS message_count,
       COALESCE(p.cnt, 0) AS profile_count
FROM rooms r
LEFT JOIN (SELECT room_id, COUNT(*) cnt FROM messages GROUP BY room_id) m
       ON m.room_id = r.room_id
LEFT JOIN (SELECT room_id, COUNT(*) cnt FROM profiles GROUP BY room_id) p
       ON p.room_id = r.room_id
ORDER BY message_count DESC;
```

多表查询示例——审计日志关联消息发送者信息展示（说明性查询，审计发生时消息已删，故按审计快照自连接自身演示多表连接语法）：

```sql
SELECT a.audit_id, a.room_id, a.sender, a.content, a.deleted_at,
       r.room_name
FROM message_audit a
LEFT JOIN rooms r ON r.room_id = a.room_id
ORDER BY a.audit_id DESC
LIMIT 20;
```

【截图：4-8 多表查询结果】

### 4.5.3 存储过程/触发器的创建

（1）触发器 trg_message_delete_audit

- 用途：对 messages 表的任何删除操作自动留痕，满足管理可追溯的非功能需求。
- 影响范围：AFTER DELETE、FOR EACH ROW。无论是管理员强制删除、用户撤回、清空聊天记录、房间恢复出厂，还是超过保留条数后的自动裁剪，每删除一行即向 message_audit 插入一条 action='DELETE' 的记录，保存被删消息的房间号、消息 ID、类型、内容、发送者、会话标识与删除时间。

```sql
DROP TRIGGER IF EXISTS trg_message_delete_audit;
CREATE TRIGGER trg_message_delete_audit
    AFTER DELETE
    ON messages
    FOR EACH ROW
INSERT INTO message_audit (room_id, message_id, type, content, sender, user_id, deleted_at, action)
VALUES (OLD.room_id, OLD.id, OLD.type, OLD.content, OLD.sender, OLD.user_id, UNIX_TIMESTAMP(), 'DELETE');
```

【截图：4-9 触发器创建成功与删除消息后审计表新增记录】

（2）存储过程一：sp_get_message_stats（消息综合统计）

- 用途：管理端"数据查询"页一次性获取三类统计结果。
- 参数：IN p_room_id VARCHAR(64)，目标房间号。
- 返回值：无返回值，输出 3 个结果集——结果集 1 总消息数；结果集 2 基于视图 v_message_stats 的按用户统计（消息数、最近/最早活跃时间，按消息数降序）；结果集 3 按类型统计的数量与百分比。

```sql
DROP PROCEDURE IF EXISTS sp_get_message_stats;
CREATE PROCEDURE sp_get_message_stats(IN p_room_id VARCHAR(64))
BEGIN
    SELECT COUNT(*) AS total_messages
    FROM messages
    WHERE room_id = p_room_id;

    SELECT *
    FROM v_message_stats
    WHERE room_id = p_room_id
    ORDER BY message_count DESC;

    SELECT m.type,
           COUNT(*) AS count,
           ROUND(COUNT(*) * 100.0 / NULLIF((SELECT COUNT(*)
                                            FROM messages
                                            WHERE room_id = p_room_id), 0), 1) AS percentage
    FROM messages m
    WHERE m.room_id = p_room_id
    GROUP BY m.type
    ORDER BY count DESC;
END;
```

调用示例：`CALL sp_get_message_stats('room_default');`

（3）存储过程二：sp_cleanup_old_messages（按条数裁剪旧消息）

- 用途：管理员修改"消息保留条数"后，删除超出部分，只保留最新的 p_max_keep 条。
- 参数：IN p_room_id 房间号；IN p_max_keep 保留条数（应用层校验 ≥10，CHECK 约束亦保证 ≥10）。
- 返回值：无；执行 DELETE，被删行逐行触发审计触发器，实现"裁剪也留痕"。

```sql
DROP PROCEDURE IF EXISTS sp_cleanup_old_messages;
CREATE PROCEDURE sp_cleanup_old_messages(IN p_room_id VARCHAR(64), IN p_max_keep INT)
BEGIN
    DELETE
    FROM messages
    WHERE room_id = p_room_id
      AND seq NOT IN (SELECT seq
                      FROM (SELECT seq
                            FROM messages
                            WHERE room_id = p_room_id
                            ORDER BY seq DESC
                            LIMIT p_max_keep) t);
END;
```

（4）存储过程三：sp_get_audit_log（查询审计日志）

- 用途：管理端"审计日志"页按时间倒序返回最近 N 条删除记录。
- 参数：IN p_room_id 房间号；IN p_limit 返回条数。
- 返回值：单结果集，message_audit 中该房间按 audit_id 降序的前 p_limit 行。

```sql
DROP PROCEDURE IF EXISTS sp_get_audit_log;
CREATE PROCEDURE sp_get_audit_log(IN p_room_id VARCHAR(64), IN p_limit INT)
BEGIN
    SELECT *
    FROM message_audit
    WHERE room_id = p_room_id
    ORDER BY audit_id DESC
    LIMIT p_limit;
END;
```

【截图：4-10 存储过程创建成功与 CALL 调用结果】

默认配置初始化（幂等插入管理员初始密码）：

```sql
INSERT INTO config (`key`, `value`)
VALUES ('admin_password', 'ADMIN')
ON DUPLICATE KEY UPDATE `value` = `value`;
```

---

# 第五章 应用程序设计

## 5.1 开发平台与总体架构

- 后台数据库：MySQL 8.0（InnoDB 引擎，utf8mb4/utf8mb4_unicode_ci），Python 驱动 PyMySQL。
- 应用程序：Python 3 + Flask 框架，单文件后端 app.py；前端 HTML/CSS/JavaScript 原生实现，实时通信使用 SSE（EventSource）；二维码使用 qrcode 库生成。
- 架构分层：路由层（Flask 视图与房间 Blueprint）→ 业务层（ChatRoom：鉴权、校验、广播）→ 数据访问层（DatabaseManager 主库 / Database 业务库，全部参数化查询）→ MySQL。每房间一个 ChatRoom 实例与一个 Broadcaster 广播器，由 RoomManager 懒加载缓存。

系统功能与任务书要求的对应关系：用户登录（5.2 节）、单条记录插入（5.4 节发送消息/创建房间）、单条记录浏览与修改（5.5 节房间配置）、表格形式浏览修改删除（5.6 节消息管理与收藏）、一张报表综合多表/多视图查询（5.7 节消息统计）。

【图5-1 系统总体架构图】

## 5.2 数据库连接模块

数据访问层封装连接建立、游标获取、增删改查与存储过程调用，全部使用 `%s` 占位符参数化查询防止 SQL 注入，DictCursor 使结果集以字典形式返回便于业务层使用。关键代码如下：

```python
def _mysql_connect(with_db=True):
    """建立到 MySQL 的连接（DictCursor，autocommit）。"""
    kwargs = dict(host=MYSQL_HOST, port=MYSQL_PORT, user=MYSQL_USER,
                  password=MYSQL_PASSWORD, charset='utf8mb4',
                  cursorclass=pymysql.cursors.DictCursor, autocommit=True)
    if with_db:
        kwargs['database'] = MYSQL_DB
    return pymysql.connect(**kwargs)

class Database:
    """单房间数据访问对象：所有语句按 room_id 过滤，保证房间隔离。"""
    def add_message(self, msg):
        return self._execute(
            'INSERT INTO messages(room_id, id, type, content, sender, user_id, '
            'is_admin, avatar, color, timestamp, file_name, file_size) '
            'VALUES(%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,%s)',
            (self.room_id, msg['id'], msg['type'], msg['content'], msg['sender'],
             msg.get('user_id'), int(msg.get('is_admin', 0)), msg.get('avatar'),
             msg.get('color'), msg.get('timestamp'), msg.get('file_name'),
             msg.get('file_size')))

    def get_recent_messages(self, limit=None):
        limit = int(limit or self.max_history)
        return self._query(
            'SELECT * FROM messages WHERE room_id=%s ORDER BY seq DESC LIMIT %s',
            (self.room_id, limit))[::-1]   # 倒序取再翻转，保证时间正序返回
```

主库 DatabaseManager 负责 rooms/config 两表，并在启动时调用 ensure_mysql_schema() 自动执行建库建表脚本，保证部署机一键初始化。收藏去重使用唯一键 + INSERT IGNORE：

```python
def add_favorite(self, user_id, msg):
    # UNIQUE(room_id,user_id,msg_id) + INSERT IGNORE：重复收藏静默忽略
    self._execute(
        'INSERT IGNORE INTO favorites(room_id, user_id, msg_id, msg_type, '
        'content, sender, file_name, file_size, created_at) '
        'VALUES(%s,%s,%s,%s,%s,%s,%s,%s,%s)',
        (self.room_id, user_id, msg['id'], msg['type'], msg['content'],
         msg['sender'], msg.get('file_name'), msg.get('file_size'), time.time()))
```

## 5.3 用户登录模块

登录对应任务书"根据对用户表的访问实现的用户登录功能"。管理员密码存储于 config 表（键 admin_password），登录时读取比对；普通用户直接写入会话角色。关键代码：

```python
@app.route('/login', methods=['POST'])
def api_login():
    data = request.get_json(silent=True) or {}
    role = data.get('role', 'user')
    if role == 'admin':
        pw = data.get('password', '')
        if pw != db_manager.get_config('admin_password', 'ADMIN'):
            return jsonify({'error': '管理员密码错误'}), 403
        session['role'] = 'admin'
        return jsonify({'success': True, 'redirect': '/rooms'})
    session['role'] = 'user'
    return jsonify({'success': True, 'redirect': '/rooms'})
```

进入房间时按会话角色决定是否以管理员身份加入（is_admin），业务层校验：

```python
@room_bp.route('/api/join', methods=['POST'])
def api_join():
    data = request.get_json(silent=True) or {}
    want_admin = bool(data.get('is_admin'))
    if want_admin and session.get('role') != 'admin':
        return jsonify({'error': '需要管理员登录'}), 403   # 防伪造管理员
    result, status = g.room.join(data.get('nickname'), data.get('device_id'),
                                 request.remote_addr, data.get('password'), want_admin)
    return jsonify(result), status
```

【截图：5-1 登录页面】【截图：5-2 管理员密码错误提示】

## 5.4 数据插入：发送聊天消息

发送消息是"对某个数据表的单条记录的插入"的典型实现：业务层完成在线、开放、禁言、类型、敏感词校验后构造消息对象，调用 Database.add_message 插入 messages 表，成功后向 SSE 广播。

```python
message = {'id': f'{time.time()}_{os.urandom(2).hex()}', 'type': msg_type,
           'content': content, 'sender': user['nickname'], 'user_id': user_id,
           'is_admin': user['is_admin'], 'avatar': user['avatar'],
           'color': user['color'], 'timestamp': time.time(),
           'file_name': file_name, 'file_size': file_size}
self.db.add_message(message)                      # INSERT INTO messages ...
self.broadcaster.broadcast({'type': 'message', 'message': message})
return {'success': True}, 200
```

加入房间时向 profiles 表插入/更新档案，同样属于单条插入（INSERT ... ON DUPLICATE KEY UPDATE 处理昵称变更）。

【截图：5-3 聊天页发送消息】

## 5.5 数据浏览与修改：房间管理

房间列表页从 rooms 表浏览数据（表格浏览），管理员可修改房间名称、开关状态、密码、限额等（单条记录修改），并同步广播给在线用户。典型修改代码：

```python
def _save_state(self):
    """把内存中的房间配置写回主库 rooms 表（UPDATE 单条记录）。"""
    self._db_manager.update_room(self.room_id,
        room_name=self.room_name, is_open=int(self.room_open),
        password=self.room_password, file_limit_mb=self.file_limit_mb,
        max_history=self.max_history, recall_time_limit=self.recall_time_limit)

def set_recall_time_limit(self, admin_id, limit):
    if not self.is_admin(admin_id):
        return {'error': '无权限'}, 403
    if not isinstance(limit, (int, float)) or limit < 0:
        return {'error': '无效的时间限制'}, 400
    self.recall_time_limit = int(limit)
    self._save_state()                            # UPDATE rooms SET recall_time_limit=...
    self.broadcaster.broadcast({'type': 'recall_time_limit',
                                'limit': self.recall_time_limit})
    return {'success': True, 'limit': self.recall_time_limit}, 200
```

修改保留条数时进一步调用存储过程完成数据裁剪：

```python
def set_max_history(self, admin_id, max_history):
    ...
    self.db.max_history = self.max_history
    self._save_state()
    self.db.cleanup_old_messages(self.max_history)   # CALL sp_cleanup_old_messages
    ...
```

【截图：5-4 房间列表页】【截图：5-5 房间配置修改】

## 5.6 表格浏览、修改与删除：消息管理

聊天页以列表形式浏览消息（表格浏览）；管理员可强制删除任意消息，用户可在时限内撤回自己的消息（删除/修改语义）；收藏夹提供另一组表格的浏览与删除。删除消息同时演示触发器联动：

```python
def admin_delete_message(self, admin_id, message_id):
    if not self.is_admin(admin_id):
        return {'error': '无权限'}, 403
    msg = self.db.get_message_by_id(message_id)      # SELECT 确认存在
    if not msg:
        return {'error': '消息不存在'}, 404
    self.db.delete_message(message_id)               # DELETE → 触发器写审计
    self.broadcaster.broadcast({'type': 'message_deleted', 'message_id': message_id})
    return {'success': True}, 200
```

撤回时限校验（0 表示禁止撤回）：

```python
elapsed = time.time() - (msg['timestamp'] or 0)
if elapsed > self.recall_time_limit:
    return {'error': f'已超过{self.recall_time_limit}秒撤回时限'}, 400
self.db.delete_message(message_id)
```

收藏删除按主键+属主双重校验防止越权：

```python
def remove_favorite(self, user_id, fav_id):
    return self._execute('DELETE FROM favorites WHERE id=%s AND user_id=%s AND room_id=%s',
                         (fav_id, user_id, self.room_id)) > 0
```

【截图：5-6 消息撤回】【截图：5-7 收藏夹列表与删除】【截图：5-8 审计日志页】

## 5.7 综合查询：消息统计报表

管理端"数据查询"页对应任务书"在一张报表中实现对若干数据表的综合查询"：后端调用存储过程，一次获得总消息数、按用户统计（视图 1）、按类型统计及百分比三组数据，前端渲染为一张综合报表。

```python
def sp_get_message_stats(self):
    with self._lock:
        with self._conn.cursor() as cur:
            cur.execute('CALL sp_get_message_stats(%s)', (self.room_id,))
            total_row = cur.fetchone()
            total = (total_row or {}).get('total_messages', 0)
            user_stats, type_stats = [], []
            if cur.nextset():
                user_stats = list(cur.fetchall() or [])
            if cur.nextset():
                type_stats = list(cur.fetchall() or [])
            return {'total_messages': total, 'user_stats': user_stats,
                    'type_stats': type_stats}
```

路由侧强制"房间内在线管理员"校验，防止统计接口被普通用户调用：

```python
@room_bp.route('/api/admin/message_stats')
def api_admin_message_stats():
    user_id = request.args.get('user_id')
    if not g.room.is_admin(user_id):
        return jsonify({'error': '无权限'}), 403
    return jsonify(g.room.db.sp_get_message_stats())
```

【截图：5-9 消息综合统计报表】

## 5.8 系统运行截图

【截图：5-10 入口登录页（含二维码）】
【截图：5-11 房间列表页（管理员视角）】
【截图：5-12 聊天页（双端消息互通）】
【截图：5-13 图片/文件消息与下载】
【截图：5-14 手机扫码进入房间】

（以上截图请在系统运行后按标注补入，建议用浏览器开发者工具或直接窗口截图，宽度不超过 12×7cm 居中排版。）

## 5.9 小组分工

本课题按任务书要求 2 人一组完成，分工如下：

| 成员 | 主要职责 | 对应报告章节 |
| --- | --- | --- |
| 成员 A（组长，姓名学号：（填写）） | 需求分析；数据库概念/逻辑/物理结构设计；sql/schema.sql 全部 DDL（约束、索引、视图、存储过程、触发器）；数据访问层 Database/DatabaseManager；统计与审计功能；测试脚本与缺陷修复 | 第一章~第四章、5.2、5.7 |
| 成员 B（姓名学号：（填写）） | Flask 路由与业务层 ChatRoom（登录鉴权、加入发消息、房间管理、禁言踢出、收藏撤回）；前端页面（登录页/房间列表/聊天页）与 SSE 实时推送；二维码与文件上传；系统联调与截图 | 5.1、5.3~5.6、5.8 |

协作方式：共同完成需求调研与 E-R 图评审；代码经双方交叉测试（冒烟用例覆盖登录、房间增删、收发消息、统计审计、恢复出厂等 40 余项）后合并；文档由双方互审定稿。

---

# 结束语

本课题设计并实现了一个基于 Flask 与 MySQL 的多房间在线匿名聊天室系统。按照"需求分析 → 概念结构设计 → 逻辑结构设计 → 物理结构设计 → 应用程序实现 → 测试"的完整流程完成了数据库课程设计的全部任务。

设计结果方面：数据库包含 7 张基本表、2 个视图、1 个触发器、3 个存储过程，通过主键、外键级联、UNIQUE、CHECK、DEFAULT 等约束保证实体完整性、参照完整性与用户定义完整性；关系模式经分析满足第三范式要求；按查询模式为各表建立了复合索引，常用查询可命中索引。SQL 编程方面：触发器实现"删除即审计"，存储过程实现综合统计、按条裁剪与审计分页查询，视图封装统计口径，体现了数据库端逻辑与应用端逻辑的合理分工。应用实现方面：完成了登录、房间管理、匿名聊天、收藏撤回、禁言踢出、统计报表等全部任务书要求功能，并通过 40 余项自动化冒烟用例验证，关键路径全部通过。

系统尚存在不足：其一，消息按整表存储，超高并发场景下可考虑按时间分区或分库；其二，SSE 单进程广播在多进程部署时需引入消息队列；其三，敏感词过滤为简单替换，准确率有限；其四，审计日志目前只保留快照，缺少归档与导出功能。后续可从分区表、Redis 发布订阅、审计归档、HTTPS 与密码加密存储等方面继续完善。

---

# 参考文献

[1] 王珊，萨师煊. 数据库系统概论（第5版）[M]. 北京: 高等教育出版社, 2014.9

[2] 王珊，张俊. 数据库系统概论（第5版）习题解析与实验指导[M]. 北京: 高等教育出版社, 2015.7

[3] 任进军，林海霞. MySQL数据库管理与开发（慕课版）[M]. 北京: 人民邮电出版社, 2022

[4] 党德鹏. MySQL数据库应用、设计与实现（第二版）[M]. 北京: 清华大学出版社, 2021.1

[5] Grinberg M. Flask Web Development[M]. 2nd ed. Sebastopol: O'Reilly Media, 2018

[6] MySQL 8.0 Reference Manual[EB/OL]. https://dev.mysql.com/doc/refman/8.0/en/

---

（文字稿完）
